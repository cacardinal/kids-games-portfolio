node_modules/
dist/
_site/
*.local
.DS_Store
*.log
.vite/

# Local profile overrides — real first names live here only, never committed.
# (`*.local` above does not match a `.ts` file, so this rule is required.)
profiles.local.ts
**/profiles.local.ts

# Private pre-public history backup — never push
*.bundle

# Firebase — never commit secrets or emulator/runtime data.
# (site/firebase-config.js is PUBLIC web config and IS committed.)
.firebase/
firebase-debug.log
firestore-debug.log
ui-debug.log
firebase-export-*/
.emulator-data/
*serviceAccount*.json
*service-account*.json
serviceAccountKey.json
.env
.env.*
.runtimeconfig.json
