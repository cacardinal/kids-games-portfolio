{
  "name": "kids-games-portfolio-sync",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Cross-device save-sync harness for the games portfolio (test/tooling root).",
  "scripts": {
    "test:sync": "firebase emulators:exec --only auth,firestore --project kids-games-portfolio \"node scripts/sync-emulator-test.mjs\""
  },
  "devDependencies": {
    "firebase": "^10.14.1",
    "jsdom": "^24.1.3"
  }
}
